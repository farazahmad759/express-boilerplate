{
  "name": "express-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon public/index.js",
    "git:merge-dev-in-main": "git checkout main && git merge dev && git push origin && git checkout dev",
    "git:merge-main-in-prod": "git checkout prod && git merge main && git push origin && git checkout dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "hbs": "^4.1.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.6"
  }
}
